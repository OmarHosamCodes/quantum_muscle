/// Enum representing the type of user.
///
/// The [UserType] enum defines two possible values:
/// - [trainer]: Represents a user who is a trainer.
/// - [trainee]: Represents a user who is a trainee.
enum UserType {
  /// Represents a user who is a trainer.
  trainer,

  /// Represents a user who is a trainee.
  trainee,
}
